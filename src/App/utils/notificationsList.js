const notifications = {
  registration: `В пароле используйте от 8 до 32 символов: строчные и прописные латинские буквы (A-z), цифры (0-9) и спец символы ( . , : ; ? ! * + % - < > @ [ ] { } / \\ _ {} $ # )`,
  eventPassed: 'Мероприятие уже прошло',
};

export default notifications;
